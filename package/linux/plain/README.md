# Plain Packaging Assets

This directory is used for non-Debian Linux packaging assets:

- source tarball output from `make dist`
- optional integration patches (for example File Roller backend patches)
